[project]
name = "ai-agents"
version = "0.1.0"
description = "A learning repo: how modern AI agents are built, one component at a time"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.70",
    "langchain>=1.3.15",
    "langchain-anthropic>=1.5.6",
    "langgraph>=1.2.11",
    "numpy>=2.4.6",
    "python-dotenv>=1.0",
    "sentence-transformers>=5.7.0",
]
